import 'package:flutter/widgets.dart';

const borderRadiusZero = BorderRadius.zero;
const borderRadius4 = BorderRadius.all(Radius.circular(4));
const borderRadius6 = BorderRadius.all(Radius.circular(6));
const borderRadius8 = BorderRadius.all(Radius.circular(8));
const borderRadius10 = BorderRadius.all(Radius.circular(10));
const borderRadius12 = BorderRadius.all(Radius.circular(12));
const borderRadius16 = BorderRadius.all(Radius.circular(16));
const borderRadius20 = BorderRadius.all(Radius.circular(20));
const borderRadius24 = BorderRadius.all(Radius.circular(24));
const borderRadius28 = BorderRadius.all(Radius.circular(28));
const borderRadius50 = BorderRadius.all(Radius.circular(50));

const borderRadiusVT10 = BorderRadius.vertical(top: Radius.circular(10));
const borderRadiusHR10 = BorderRadius.horizontal(right: Radius.circular(10));
const borderRadiusHL10 = BorderRadius.horizontal(left: Radius.circular(10));
