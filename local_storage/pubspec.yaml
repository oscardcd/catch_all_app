name: local_storage
description: package for local storage for catch all app.
version: 1.0.0

environment:
  sdk: ^3.11.0

dependencies:
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path: ^1.9.0

dev_dependencies:
  build_runner: ^2.4.13
  hive_generator: ^2.0.1
  lints: ^6.0.0
  test: ^1.25.6
